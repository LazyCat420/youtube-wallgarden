# Extension build artifacts
extension.crx
extension.pem

# OS files
.DS_Store
Thumbs.db
desktop.ini

# IDE
.vscode/
.idea/
*.code-workspace

# Node (if added later)
node_modules/
package-lock.json

# Build artifacts (generated by Dockerfile minification stage)
app/app.min.js
app/app.min.css

# Python venv — the Playwright interpreter for `npm run test:smoke`. Was the
# shared ../scraper-service/.venv, now a dedicated local venv (that repo is gone).
.venv/

# Gemini agent artifacts
.gemini/

# LLM debug dumps
raw_*.txt
